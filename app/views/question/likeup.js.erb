
document.body.addEventListener('ajax:success', function(event) {
    var detail = event.detail;
    var data = detail[0], status = detail[1],  xhr = detail[2];
    document.getElementById('LikeCount').innerHTML = "<%= @question.like %>"
    document.getElementById('LikeStatus').innerHTML ="<%= escape_javascript(link_to 'Я передумал',likedown_path(id: @question.id), method: :post, remote: true)  %>"
})

